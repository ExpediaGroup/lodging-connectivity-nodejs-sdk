{
  "name": "examples",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "dev": "ts-node -r tsconfig-paths/register ./src/index.ts",
    "clean": "rimraf dist",
    "lint": "eslint 'src/**/*.ts'",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "@expediagroup/lodging-connectivity-sdk": "workspace:*"
  }
}
